# ============================================================
# CI — Build & Test (multi-plateforme)
# ============================================================
# Ce workflow s'execute a chaque push sur main ou PR.
# Il compile et teste sur Linux, Windows et macOS en parallele.
#
# La section "strategy.matrix" definit les variantes :
# chaque combinaison genere un job independant.

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

name: CI

jobs:
  build-and-test:
    strategy:
      fail-fast: false    # ne pas arreter les autres OS si l'un echoue
      matrix:
        include:
          # --- Linux : compilateur clang depuis conda ---
          - os: ubuntu-latest
            extra-packages: >-
              clangxx
              clang-tools
              llvm-tools
              lld
            shell: micromamba-shell {0}

          # --- Windows : compilateur MSVC (pre-installe sur le runner) ---
          - os: windows-latest
            extra-packages: ""
            shell: cmd /C call {0}

          # --- macOS : compilateur Apple clang (pre-installe) ---
          - os: macos-latest
            extra-packages: ""
            shell: micromamba-shell {0}

    runs-on: ${{ matrix.os }}
    defaults:
      run:
        shell: ${{ matrix.shell }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup micromamba environment
        uses: mamba-org/setup-micromamba@v2
        with:
          micromamba-version: latest
          environment-name: ci
          condarc: |
            channels:
              - conda-forge
          create-args: >-
            python=3.13
            cmake
            ninja
            pybind11
            occt
            eigen
            numpy
            pythreejs
            pybind11-stubgen
            pytest
            ${{ matrix.extra-packages }}
          cache-environment: true
          # Sur Windows, init cmd en plus de bash
          init-shell: >-
            bash
            ${{ matrix.os == 'windows-latest' && 'cmd.exe' || '' }}

      # Sur Windows avec cmd, CONDA_PREFIX est deja defini par micromamba init.
      # Sur Linux/macOS avec micromamba-shell, $CONDA_PREFIX est dans le shell.
      - name: Configure
        run: >-
          cmake -B build -S . -G Ninja
          -DCMAKE_BUILD_TYPE=Release
          -DMOD3D_USE_GBS=OFF
          -DCMAKE_INSTALL_PREFIX=$CONDA_PREFIX

      - name: Build
        run: cmake --build build --parallel

      - name: Install
        run: cmake --install build

      - name: Test
        run: pytest tests/ -v
