#pragma once

// #include <TopoDS_Shape.hxx>

// #ifdef _WIN32
//     #ifdef occt_extend_EXPORTS
//         #define OCCT_EXTEND_API __declspec(dllexport)
//     #else
//         #define OCCT_EXTEND_API __declspec(dllimport)
//     #endif
// #else
//     #define OCCT_EXTEND_API
// #endif

// OCCT_EXTEND_API TopoDS_Shape union_faces(const TopoDS_Shape& shape);
// void TopoDS_Shape union_faces(const TopoDS_Shape& shape);
